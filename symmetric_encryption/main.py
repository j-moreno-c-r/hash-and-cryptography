import tk

"""
def main():
    # Get a password and data to encrypt from the user
    password = input('Enter a password: ')
    data = input('Enter data to encrypt: ').encode('utf-8')

    # Encrypt the data
    encrypted = encrypt_data(password, data)
    print('Encrypted:', binascii.hexlify(encrypted))

    # Get the password to decrypt the data
    password = input('Enter the password to decrypt the data: ')

    # Decrypt the data
    decrypted = decrypt_data(password, encrypted)
    print('Decrypted:', decrypted.decode())

"""


def main():
    tk.root.mainloop()
       
if __name__ == "__main__":
    main()